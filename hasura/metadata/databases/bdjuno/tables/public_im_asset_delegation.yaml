table:
  name: im_asset_delegation
  schema: public
select_permissions:
- permission:
    allow_aggregations: false
    columns:
    - staker_id
    - delegated
    - pending_undelegation
    - lifetime_slashed
    filter: {}
    limit: 10
  role: anonymous
